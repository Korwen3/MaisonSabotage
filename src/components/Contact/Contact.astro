---
import contact from "../../assets/images/contact.jpg";
const contactImg = `url(${contact.src})`;
---

<section>
    <header>
        <h1>Contact</h1>
        <p>
            Pour toutes demandes d'information, vous avez la possibilité
            d'envoyer un DM sur instagram à <a class="link-before" href="https://www.instagram.com/maison_sabotage">@maison_sabotage</a> ou d'envoyez un
            mail via <a class="link-before" href="mailto:maisonsabotage@gmail.com">maisonsabotage@gmail.com</a> ou bien de passer par le
            formulaire ci-dessous.
        </p>
    </header>

    <div>
        <div></div>
    </div>

    <form
        action="https://formspree.io/f/xqaelnab"
        method="POST"
        autocomplete="on"
    >
        <label for="identity">Nom et prénom (requis)</label>
        <input
            id="identity"
            type="text"
            name="identity"
            placeholder="John Snow"
            required
        />

        <label for="phone">Numéro de téléphone</label>
        <input
            id="phone"
            type="tel"
            name="phone"
            placeholder="+337 XX XX XX XX"
        />

        <label for="email">Email (requis)</label>
        <input
            id="email"
            type="email"
            name="email"
            placeholder="votre.email@hellokitty.fr"
            required
        />

        <label for="message">Votre message (requis)</label>
        <textarea
            id="message"
            name="message"
            placeholder="Votre message..."
            required></textarea>

        <button type="submit">Send</button>
    </form>
</section>

<style define:vars={{ contactImg }}>
    section {
        --radius: 0.5rem;

        padding-inline: 4rem;
        padding-bottom: 2rem;
        row-gap: 2rem;
        display: grid;
        grid-template-columns: 1fr 1fr;


        & > header {
            grid-column: span 2;
            color: var(--primary-200);

            h1 {
                font-size: 6.8rem;
            }

            p {
                margin-inline: auto;
                text-align: center;
            }
        }

        & > div:first-of-type {
            width: 100%;
            border-radius: var(--radius);
            position: relative;

            background-color: var(--primary-400);
            background-image: var(--contactImg);
            background-size: cover;
            background-blend-mode: luminosity;
            overflow: hidden;

            &::before {
                position: absolute;
                content: "";
                width: 100%;
                height: 100%;
                top: 0;
                left: 0;
                background-color: rgba(var(--color-token-400), 0.5);
                z-index: -1;
            }
        }

        & > form {
            margin-block: 3rem;
            padding: 3rem;
            display: grid;
            gap: 0.3rem;
            border-radius: 0 var(--radius) var(--radius) 0;

            background-image: radial-gradient(100% 75% at 50% 100%, var(--primary-700) 0%, var(--primary-900) 100%);

            label {
                color: var(--primary-200);
                letter-spacing: 2px;
            }

            input,
            textarea {
                background-color: inherit;
                border: none;
                margin-bottom: 1rem;
                outline: 1px solid;
                padding: 1rem;
                color: var(--primary-200);

                &::placeholder {
                    color: var(--primary-400);
                }
            }

            button {
                margin-top: 2rem;
                width: fit-content;
                padding: 0.5rem 2rem;
                justify-self: flex-end;
                border-radius: 100svh;
                border: none;
                color: var(--primary-200);
                background-color: var(--primary-700);
                font-size: 1.2rem;
                font-family: "Yataghan", sans-serif;
            }
        }
    }
</style>
